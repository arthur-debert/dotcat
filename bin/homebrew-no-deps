#! /usr/bin/env bash
#this scripts uses poet to get the checksum

mkdir -p "dist"
poet --single dotcat | sed '1d;$d' > dist/dotcat-formula-middle
cat bin/parts/dotcat-formula-top dist/dotcat-formula-middle bin/parts/dotcat-formula-bottom > Formula/dotcat.rb

